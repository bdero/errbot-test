# Create a copy of this file and rename it to 'secrets.yaml'

username: test
password: testing

# Uncomment to override XMPP server config (if different than specified in username)
#server:
#  domain: kode.im
#  port: 5222
